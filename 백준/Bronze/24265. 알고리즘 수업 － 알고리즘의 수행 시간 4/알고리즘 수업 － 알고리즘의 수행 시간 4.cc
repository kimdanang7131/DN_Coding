#include <iostream>
#include <vector>

using namespace std;

long long n;

int main()
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	cin >> n;
	
	cout << n* (n - 1) / 2<< endl << 2 << endl;
	

	return 0;
}