#include <iostream>
#include <vector>

using namespace std;

long long n;

int main()
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	cin >> n;
	
	cout << n * n * n << endl << 3 << endl;
	

	return 0;
}